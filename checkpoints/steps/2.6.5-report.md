# Étape 2.6.5 — Trace documentaire (commit déjà réalisé)

> **Nature de ce document** : trace documentaire a posteriori du commit
> `ff19f6e`, déjà présent dans l'historique Git avant la rédaction de ce
> fichier. Ce n'est **pas** une nouvelle validation fonctionnelle, ni un
> audit, ni une réévaluation du contenu de 2.6.5 — uniquement la
> consignation fidèle de ce que ce commit contient et de son statut vis-à-vis
> du système de checkpoint. Aucune interprétation nouvelle n'est ajoutée par
> rapport à ce que montrent `git show ff19f6e` et `checkpoints/README.md`.

## A. Commit concerné

- **Hash** : `ff19f6e82d3edf3c78cb7c64b6b4caed37a2010e`
- **Message** : `WIP: etape 2.6.5 - extension du referentiel AttributeDefinition aux activites du Core ERP`
- **Fichiers modifiés** (`git show --stat ff19f6e`) :
  - `database/seeders/AttributeDefinitionSeeder.php`
  - `tests/Feature/AttributeDefinitionSeederTest.php`

## B. Contenu (tel que décrit par le message de commit lui-même)

Extension du seeder `AttributeDefinitionSeeder` (attributs de niveau
produit/variante), conformément à la matrice métier validée :

- `color` : devient TRANSVERSAL (`activity` `'sport'` → `null`) — seule
  définition existante modifiée, jamais dupliquée (contrainte UNIQUE sur
  `code`).
- `size`, `version` : restent scopés `'sport'`, inchangés.
- Bébé : `tranche_age` (product/select), `taille_bebe` (variant/select).
- Moto : `modele_compatible` (product/text), `cylindree` (variant/select).
- Artisanat du Maroc : `matiere` (product/text), `region_origine`
  (product/select), `dimension` (variant/text).
- VTC : aucune définition ajoutée (`VtcRide`, sans référence à
  `Product`/`ProductVariant`/`StockMovement`).

Total après ce commit : 13 définitions dans `attribute_definitions`. Aucune
activité traitée comme centrale ou par défaut. Aucun formulaire Filament
modifié, aucune migration, aucune suppression de colonne ; `products.version`
/ `club_id` / `competition_id` non touchés par ce commit.

Tests mentionnés dans le message de commit :
`php artisan test --filter="AttributeDefinitionSeederTest|AttributeCrossCheckTest|AttributeDualWriteTest|Tier1AttributeFoundationTest|ProductTailleActivityGuardTest"`
→ 37/37 PASS, 231 assertions (résultat déclaré par le commit, non ré-exécuté
par ce document).

Le message de commit précise explicitement que `checkpoints/state.json` et
`CHECKPOINTS.md` ont été volontairement exclus de ce commit.

## C. Statut vis-à-vis du système de checkpoint

Ce commit utilise le format `WIP: <résumé>`, l'un des trois formats acceptés
par le hook `commit-msg` (`checkpoints/README.md`, section « Trois formats de
message acceptés »). Par conception du système, ce format désigne du
« travail en cours, hors checkpoint, toujours accepté sans vérification de
périmètre » :

- aucun manifeste `checkpoints/steps/2.6.5.json` n'a été créé ni déverrouillé
  par l'opérateur humain (vérifié : ce fichier n'existe pas dans le dépôt) ;
- aucune commande `checkpoint.ps1 validate -Step 2.6.5` n'a été exécutée ;
- aucune commande `checkpoint.ps1 authorize -Step 2.6.5` n'a été exécutée ;
- aucune commande `checkpoint.ps1 commit -Step 2.6.5` n'a été exécutée ;
- `checkpoints/state.json` ne référence pas 2.6.5 (`last_committed_step`
  reste à `"2.6.4"` dans sa version déjà présente dans l'arbre de travail au
  moment de la rédaction de ce document) ;
- aucun tag `checkpoint/2.6.5` n'a été créé.

Ce n'est pas une anomalie ni un contournement au sens de l'audit
`bypass-audit.jsonl` (qui ne s'applique qu'aux commits au format
`checkpoint(step-...)` illégitimes) : un commit `WIP:` est, par définition du
système, en dehors du suivi par étape. Ce document a pour seul but de rendre
ce fait explicite et traçable avant qu'une étape suivante (2.6.6) ne soit
engagée, sans modifier rétroactivement le statut de 2.6.5 ni le système de
checkpoint lui-même.

## D. Ce que ce document ne fait pas

- Il ne crée, ne déverrouille ni ne modifie aucun manifeste
  `checkpoints/steps/*.json`.
- Il ne modifie pas `checkpoints/state.json`.
- Il ne modifie aucun fichier de code, de migration, de seeder, ou de
  formulaire.
- Il ne ré-exécute aucun test et ne formule aucun nouveau verdict de
  validation sur le contenu de 2.6.5.
- Il ne prend aucune décision sur `products.version`, `club_id` ou
  `competition_id` : ces sujets restent non traités, comme déjà consigné
  dans `checkpoints/steps/2.6.3-report.md`.

## E. Conformité

- Aucune activité (Sport, VTC, Bébé, Moto, Artisanat du Maroc) n'est
  présentée comme centrale ou par défaut dans ce document.
- Aucune colonne dédiée existante n'est mentionnée comme candidate à une
  suppression.
- Ce document est lui-même un fichier sous `checkpoints/`, conforme au
  format de commit `docs(checkpoint): <résumé>` (`checkpoints/README.md`,
  format 3).
